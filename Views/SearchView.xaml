<UserControl x:Class="Forecaster.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             Loaded="View_Loaded">
    <StackPanel VerticalAlignment="Center">
        <TextBox
            FontFamily="{DynamicResource OpenSansLight}" FontSize="25" Width="75" Margin="0 0 0 20" Name="SearchCityBar"
            Controls:TextBoxHelper.Watermark="Enter city name" Controls:TextBoxHelper.UseFloatingWatermark="True" />
        <Button
            FontFamily="{DynamicResource OpenSansLight}" FontSize="1" Opacity="0" Padding="30 5" Name="SearchButton"
            Controls:ControlsHelper.ContentCharacterCasing="Normal" HorizontalAlignment="Center" Content="Search" />
    </StackPanel>
</UserControl>
